#include <bits/stdc++.h>
#define ll long long int
using namespace std;
//ربنا أكرم من أي حد
//Jerry&Tom
//O_SOFTWARE
//هو انا هحل؟انتو بتوعي وانا بتعاكوو يتهدوني يهدكو
void omar(){
ios::sync_with_stdio(0);
cin.tie(0);

}
int main()
{//abcdefa
    omar();
int arr[50];
int size,pos,i;
cout<<"enter the size of array: "<<endl;
cin>>size;
cout<<"enter the elements of array: "<<endl;
for( i=0;i<size;i++)
{
    cin>>arr[i];
    }
    cout<<"enter the pos you want to shift"<<endl;
cin>>pos;
while(pos<=0&&pos>size-1)
{
    cout<<"invalid,enter rigth pos";

    cin>>pos;
}
for(i=pos-1;i<=size-1;i++)
{
    arr[i]=arr[i+1];
}
size--;
for( i=0;i<size;i++)
   cout<<arr[i]<<" ";
    return 0;
}
